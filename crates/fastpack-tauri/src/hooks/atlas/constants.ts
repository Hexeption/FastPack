/** Layout and zoom constants for the atlas canvas. */

/** Pixel gap between sheets when multiple sheets are displayed side by side. */
export const SHEET_GAP = 16;
/** Minimum zoom level. */
export const ZOOM_MIN = 0.05;
/** Maximum zoom level. */
export const ZOOM_MAX = 64;
/** Multiplier applied per scroll tick when zooming in. */
export const ZOOM_IN_FACTOR = 1.1;
/** Multiplier applied per scroll tick when zooming out. */
export const ZOOM_OUT_FACTOR = 0.9;
